import React from "react";
import uniqid from "uniqid";
import Quill from "quill";

const AddCourse = () => {
  const quillRef = useRef(null);

  return (
    <div>
      <h1>AddCourse AddCourse</h1>
    </div>
  );
};

export default AddCourse;
